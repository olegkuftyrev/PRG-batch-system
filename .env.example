# PRG Batch System — copy to .env and set values
# Used by: docker compose, api (Adonis), and optionally web (Vite) for API URL

# --- Postgres (Docker and API) ---
POSTGRES_USER=prg
POSTGRES_PASSWORD=prg_secret
POSTGRES_DB=prg_batch
POSTGRES_PORT=5432

# --- API (Adonis) — when running on host, point to Docker Postgres ---
# In Docker: api uses DB_HOST=postgres. On host: DB_HOST=127.0.0.1 or localhost
NODE_ENV=development
API_PORT=3333
DB_HOST=127.0.0.1
DB_PORT=5432
DB_USER=prg
DB_PASSWORD=prg_secret
DB_DATABASE=prg_batch

# --- Frontend (Vite) — optional; for build-time API URL ---
# VITE_API_URL=http://localhost:3333
